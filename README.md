### web本地存储,可设置过期时间，无需转换value数据类型
### 使用：$.localStorage
### 传值：{
    name:'',                        ** key值
    value:'',                       ** value值
    expires:"",                     ** 过期时间（时间段，毫秒级）
}
